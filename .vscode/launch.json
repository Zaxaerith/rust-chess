{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Rust 运行国际象棋 (release)",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}/target/release/chess_rust.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cargo build --release - 编译发布版",
      "stopAtEntry": false
    }
  ]
}
